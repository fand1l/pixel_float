org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# Configuration cache is deliberately OFF for now: AGP 9 + KSP is exactly the
# combination where incompatibilities surface, and a CI-only project cannot
# afford an unexplained configuration failure. Turn it on as its own commit.
# org.gradle.configuration-cache=true
